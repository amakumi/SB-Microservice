package com.springboot2.hr_app.config;

/*import com.github.benmanes.caffeine.cache.Cache;
import com.github.benmanes.caffeine.cache.stats.CacheStats;
import org.springframework.cache.CacheManager;
import org.springframework.context.annotation.Configuration;

@Configuration
public class cacheStats {

    private static final CacheManager cacheManager = null;

    public static void printStats(Cache cacheName) {
        CacheStats cacheStats = cacheName.stats();

        long evictionCount = cacheStats.evictionCount();
        long hitCount = cacheStats.hitCount();
        long missCount = cacheStats.missCount();

        System.out.println("-------------------------------------");
        System.out.println("\n Cache Stats");
        System.out.println("EVICTION COUNT: "+ evictionCount);
        System.out.println("HIT COUNT: "+ hitCount);
        System.out.println("MISSED COUNT: "+ missCount);

        System.out.println("-------------------------------------");

    }*/





   /*public CacheStats getStats() {
       org.springframework.cache.Cache cache = caffeineConfig.cacheManager().getCache("cache");
       Cache cache = (Cache) cache.geti
   }*/

//
// }
